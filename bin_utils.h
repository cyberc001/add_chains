#ifndef BIN_UTILS_H
#define BIN_UTILS_H

/* Calculates power of unsigned integers using binary algorithm. */
unsigned upow(unsigned x, unsigned n);
/* Returns length of unsigned integer in digits in binary base. */
unsigned bin_length(unsigned x);

#endif
